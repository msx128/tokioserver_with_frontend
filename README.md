# tokioserver_with_frontend
Asynchronous HTTP server written in Rust using Tokio.

Features:
- TCP socket handling
- Basic HTTP request parsing
- Static file serving
- Routing
- JSON request processing
- MySQL integration through SQLx
- Shared application state

Built as a learning project to understand networking,
HTTP, and asynchronous I/O from first principles.
